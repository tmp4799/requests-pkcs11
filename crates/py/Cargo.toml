[package]
name = "requests-pkcs11"
version = "0.1.0"
edition = "2021"

[lib]
name = "requests_pkcs11"
crate-type = ["cdylib"]
path = "src/lib.rs"

[dependencies]
pyo3 = { version = "0.29.0", features = ["extension-module"] }
requests-pkcs11-core = { path = "../core" }
